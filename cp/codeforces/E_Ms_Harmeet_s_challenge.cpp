#include<bits/stdc++.h>
using namespace std;
#define int long long

/* (╯°□°)╯︵ ┻━┻ */

void solve()
{
    int p,q;
    cin >> p >> q;
    
    int Y[p][q];
    for(int i=0; i<p; i++) {
        for(int j=0; j<q; j++) {
            cin >> Y[i][j];
        }
    }

    // Yij is or of all elements in ith row & jth col in X
    // if Yij == 1, then atleast one '1' in ith row and jth col

    
    
}

int32_t main()
{
    int testCases = 1;
    // cin >> testCases;
    void solve();

    while(testCases--)
    {
        solve();
    }

    return 0;
}